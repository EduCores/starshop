.next
node_modules
data/users.json
.env
.env.local
dev-server-*.log
*.tsbuildinfo

# Base de datos SQLite local (regenerable con npm run db:seed) + archivos WAL
data/*.db
data/*.db-wal
data/*.db-shm

# Órdenes persistidas localmente (sensibles: datos de clientes)
data/orders.json

# Salida de graphify (grafo de conocimiento, regenerable con /draw)
graphify-out/

# Artefactos de tests e2e (Playwright)
test-results/
playwright-report/

# Logs de servidor de pruebas
start-*.log

# Script de debug temporal
debug-zonas.mjs
